---
import Base from "../layouts/Base.astro";
---
<Base title="Obrigada!" description="Cadastro recebido." lang="pt-BR">
  <section class="container py-16 text-center">
    <h1 class="text-3xl font-semibold">Obrigada! 💚</h1>
    <p class="mt-3 text-gray-700 dark:text-gray-300">Recebemos seu cadastro. Em breve entraremos em contato.</p>
    <div class="mt-8">
      <a href="/" class="btn">Voltar à página inicial</a>
    </div>
  </section>
  <script define:vars={{ sendTo: import.meta.env.PUBLIC_GOOGLE_ADS_SEND_TO }}>
    if (typeof window !== 'undefined' && window.gtag && sendTo) {
      // Conversão Google Ads (page-load em /obrigada)
      window.gtag('event', 'conversion', { send_to: sendTo });
    }
  </script>
</Base>
